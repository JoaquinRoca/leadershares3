%h1 Sign in to Leadershares

- if user_signed_in?
  %p Hello #{current_user.email}

  = link_to 'sign_out', destroy_user_session_path, method: :delete

- else
  = link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2)
